{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build",
            "type": "shell",
            "command": "mkdir -p build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && make clean && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}